const form=document.querySelector('#leader-request'),button=document.querySelector('#request-submit'),notice=document.querySelector('#request-notice');let id=crypto.randomUUID();
fetch('/api/care/config').then(r=>r.json()).then(d=>{button.disabled=!d.ready;notice.textContent=d.ready?'Your request will be saved for approved leaders and an email notification will be attempted.':'Online requests are not open yet while leader email routing is being connected. Please contact your usual leader or use the direct provider contacts.'}).catch(()=>{notice.textContent='Request service is unavailable. Use the direct provider contacts or your usual leader.'});
form.onsubmit=async e=>{e.preventDefault();button.disabled=true;notice.textContent='Saving your request…';try{const body={...Object.fromEntries(new FormData(form)),consent:form.elements.consent.checked,id};const r=await fetch('/api/care/requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw Error(d.error||'Unable to save.');notice.textContent='Saved. Reference: '+d.id+'. '+(d.emailState==='accepted'?'The email provider accepted the leader notification. This does not confirm a leader has read it.':'The email notification is not confirmed. Contact your usual leader and give them this reference.');form.hidden=true}catch(err){notice.textContent=err.message;button.disabled=false}};
