(()=>{const form=document.querySelector('#ai-form'),state=document.querySelector('#ai-state'),result=document.querySelector('#ai-result'),button=form.querySelector('button');const names={ride:'Rides to work',child:'Childcare',health:'Direct2Care access',uniform:'Uniform requests',food:'Food, bills and housing',growth:'Education and scholarships',hr:'Workplace support'};
fetch('/api/care/config').then(r=>r.json()).then(d=>{button.disabled=!d.aiReady;state.textContent=d.aiReady?'Groq can help match a short question to our resource list. It does not book services.':'AI setup is pending. All topic buttons and direct contacts still work.'}).catch(()=>{state.textContent='Use the topic buttons above while the helper is unavailable.'});
form.onsubmit=async e=>{e.preventDefault();button.disabled=true;result.textContent='Finding a resource…';try{const r=await fetch('/api/care/assist',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:form.elements.question.value,consent:form.elements.consent.checked})});const d=await r.json();if(!r.ok)throw Error(d.error);result.replaceChildren();if(d.reason==='urgent'){const p=document.createElement('p');p.textContent='For immediate danger call 911. For suicide or mental health crisis support call or text 988.';result.append(p);return}const note=document.createElement('p');note.textContent=d.mode==='groq'?'Choose the resource that fits:':d.reason==='personal-details'?'Your question was not sent to AI because it may contain personal details. Use these resources or the topic buttons.':'Using direct topic matching. Choose a resource or use the buttons above.';result.append(note);for(const id of d.topics){if(!names[id])continue;const b=document.createElement('button');b.type='button';b.className='secondary';b.dataset.resource=id;b.textContent=names[id];result.append(b)}if(!d.topics.length){const a=document.createElement('a');a.href='benefits.html';a.textContent='Browse all local resources';result.append(a)}}catch(err){result.textContent='The helper is unavailable. Use the topic buttons or local resource directory.'}finally{button.disabled=false}};
})();
