# All values configured separately for Preview and Production. No real values here.
APP_ORIGIN=
DATABASE_URL=
TEAM_SESSION_SECRET=
TEAM_PASSWORD_HASH=
# JSON map of approved lowercase leader email -> scrypt password hash
LEADER_PASSWORD_HASHES=
RESEND_API_KEY=
NOTIFY_FROM=
GROQ_ENABLED=false
GROQ_API_KEY=
GROQ_MODEL=
# Migration only; never configured in browser or public assets
MIGRATION_INPUT=
MIGRATION_CONFIRM=
